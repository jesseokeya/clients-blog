<!-- Page Header -->
<header class="masthead" style="background-image: url('<%= result.images[0].url %>')">
  <div class="overlay"></div>
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-md-10 mx-auto">
        <div class="post-heading">
          <h1><%= result.title %></h1>
          <h2 class="subheading">
            <%= result.subheading %>
          </h2>
          <span class="meta">Posted by
                <a href="#"><%= result.author %></a>
                on <%= result.date.toDateString() %></span>
          <br />
          <div id="editButton" class="text-center text-white"></div>
        </div>
      </div>
    </div>
  </div>
</header>

<!-- Post Content -->
<article>
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-md-10 mx-auto">

        <%for(let i = 0; i < result.body.length; i++){%>
          <%if(result.body[i] === '[insert image]'){%>
            <%if(result.images[1]){%>
            <a href="#">
                <img class="img-fluid" src="<%=result.images[1].url%>" alt="postImage">
            </a>
            <span class="caption text-muted">
               <%}%>
              <%=result.heading%>
            </span>
          <%}%>
          <%if(result.body[i] !== '[insert image]'){%>
              <p> <%= result.body[i] %> </p>
          <%}%>
        <%}%>

                    <!-- <blockquote class="blockquote">The dreams of yesterday are the hopes of today and the reality of tomorrow. Science has not yet mastered prophecy. We predict too much for the next year and yet far too little for the next ten.</blockquote> -->

                    <!-- <h2 class="section-heading">Reaching for the Stars</h2> -->

      </div>
    </div>
  </div>
</article>
